{
  "name": "titan",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "cd client && npm",
    "start": "npm run start-client & npm run start-server",
    "start-client": "cd client && npm start",
    "start-server": "cd server && npm start",
    "setup": "cd client && npm install && cd ../server && npm install && mysql -u root -p < create_db.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mysql2": "^3.12.0"
  },
  "devDependencies": {
    "cypress": "^14.0.3",
    "jest": "^29.7.0",
    "react-testing-library": "^8.0.1",
    "supertest": "^7.0.0"
  }
}
